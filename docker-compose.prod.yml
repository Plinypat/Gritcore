version: '3.9'

services:
  postgres:
    ports: []
    restart: unless-stopped

  redis:
    ports: []
    restart: unless-stopped

  api:
    restart: unless-stopped
    environment:
      NODE_ENV: production
